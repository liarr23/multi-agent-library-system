# 多Agent图书管理系统 - 使用说明

## 🚀 快速启动

### 方法一：使用批处理文件（推荐）
1. 双击运行 `启动服务.bat`
2. 等待看到 "多Agent图书管理系统 v1.0.0 已启动" 信息
3. 打开浏览器访问：http://localhost:8000/docs

### 方法二：手动启动
1. 打开命令提示符（CMD）
2. 切换到项目目录：
   ```cmd
   cd C:\Users\liar\miclaw\project\multi-agent-library-system
   ```
3. 启动服务：
   ```cmd
   python run.py
   ```
4. 打开浏览器访问：http://localhost:8000/docs

## 📖 API文档

启动后，系统提供以下API接口：

### 图书管理
- `GET /api/books/` - 获取图书列表（支持搜索和分页）
- `POST /api/books/` - 创建新图书
- `GET /api/books/{book_id}` - 获取图书详情
- `PUT /api/books/{book_id}` - 更新图书信息
- `DELETE /api/books/{book_id}` - 删除图书
- `GET /api/books/{book_id}/similar` - 获取相似图书

### 用户管理
- `GET /api/users/` - 获取用户列表
- `POST /api/users/` - 创建新用户
- `GET /api/users/{user_id}` - 获取用户详情
- `GET /api/users/{user_id}/borrow-history` - 获取借阅历史
- `GET /api/users/{user_id}/recommendations` - 获取个性化推荐

### 借阅管理
- `POST /api/borrows/` - 借阅图书
- `POST /api/borrows/return` - 归还图书
- `POST /api/borrows/renew` - 续借图书
- `GET /api/borrows/user/{user_id}` - 获取用户借阅记录
- `GET /api/borrows/overdue` - 获取逾期图书
- `POST /api/borrows/check-overdue` - 检查逾期图书

### 数据分析
- `GET /api/analytics/overview` - 获取系统概览
- `GET /api/analytics/popular` - 获取热门图书
- `GET /api/analytics/trends` - 获取借阅趋势
- `GET /api/analytics/category-stats` - 获取分类统计
- `POST /api/analytics/report` - 生成分析报告
- `GET /api/analytics/user-activity` - 获取用户活跃度
- `GET /api/analytics/agent-status` - 获取Agent状态

## 🧪 测试API

### 使用测试脚本
双击运行 `测试API.bat` 可以快速测试API连接。

### 使用curl命令
```cmd
# 健康检查
curl http://localhost:8000/health

# 获取图书列表
curl http://localhost:8000/api/books/

# 搜索图书
curl "http://localhost:8000/api/books/?keyword=Python"

# 获取系统概览
curl http://localhost:8000/api/analytics/overview
```

### 使用Swagger UI
1. 打开浏览器访问：http://localhost:8000/docs
2. 在Swagger界面中可以直接测试所有API接口
3. 点击某个接口，然后点击 "Try it out" 按钮进行测试

## 📊 示例数据

系统已预置以下示例数据：

### 图书
- Python编程从入门到实践 (ISBN: 978-7-115-42802-8)
- JavaScript高级程序设计 (ISBN: 978-7-115-54563-7)
- 数据结构与算法分析 (ISBN: 978-7-115-43054-2)
- 人工智能导论 (ISBN: 978-7-302-51734-6)

### 用户
- 张三 (student001) - 学生
- 李四 (teacher001) - 教师

## 🔧 故障排除

### 端口被占用
如果8000端口被占用，可以修改 `app/config.py` 中的端口配置：
```python
PORT: int = 8001  # 改为其他端口
```

### 数据库问题
如果遇到数据库问题，可以删除 `library.db` 文件，系统会自动重新创建。

### 依赖问题
如果缺少依赖，运行：
```cmd
pip install -r requirements.txt
```

## 📁 项目结构

```
multi-agent-library-system/
├── app/                    # 应用代码
│   ├── agents/            # Agent实现
│   ├── api/               # API路由
│   ├── models/            # 数据模型
│   ├── routers/           # 路由定义
│   ├── schemas/           # Pydantic schemas
│   ├── config.py          # 配置文件
│   ├── database.py        # 数据库配置
│   └── main.py            # 主应用
├── 启动服务.bat           # 启动脚本
├── 测试API.bat            # 测试脚本
├── run.py                 # 运行脚本
├── requirements.txt       # 依赖列表
└── README.md             # 项目说明
```

## 🎯 功能特点

1. **多Agent架构**：使用协调器模式管理多个专业Agent
2. **智能推荐**：基于用户行为的个性化图书推荐
3. **数据分析**：全面的借阅数据统计和分析
4. **RESTful API**：标准的REST接口，易于集成
5. **Swagger文档**：自动生成的交互式API文档

## 📞 技术支持

如有问题，请检查：
1. Python版本是否为3.8+
2. 所有依赖是否已正确安装
3. 端口8000是否被其他程序占用
4. 数据库文件是否有写入权限